package quiz;

/**
 * Given an array of integers, find the sequence and the sub-array with the highest sum
 */
public class SubArraysAndSubSequences {
    public Result calculate(Integer[] array) {
        return new Result(0, 0);
    }
}
